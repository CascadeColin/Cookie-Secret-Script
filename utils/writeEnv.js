// const fn = require("./functions");

// const writeEnvFile = async (fileName, varName) => {
//   const key = await fn.keyGenerator();
//   // format: "VARNAME: SHA-512 key"
//   const text = `${varName.toUpperCase()}: ${key}`;
//   fn.fileWriter(fileName, text);
// };

// writeEnvFile(".env", "secret");